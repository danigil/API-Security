We decided to use the same model for all datasets,  
to run put datasets in 'datasets' folder together with the jupyter notebook  
and create a 'predictions' folder as well for the output eval predictions
